[pytest]
markers =
    integration: tests that hit real live external APIs over the network (Census geocoder, Socrata, ArcGIS). Run everything with `pytest`; run only fast/offline tests with `pytest -m "not integration"`.
testpaths = tests
